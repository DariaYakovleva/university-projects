#include <iostream>
#include <cstdio>
#include <iostream>
#include <cmath>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

const int M = 30;
const int N = 200010;

string s;
int n;

int main() {
	freopen("tree.in", "r", stdin);
	freopen("tree.out", "w", stdout);
	getline(cin, s);
	s += (char)('a' - 1);
	n = (int)s.size();
	init();
	

	return 0;
}
